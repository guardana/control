// Package probeadapter is part of the dependency rule's negative fixture,
// planted at adapters/probeadapter: a package in a tree the rule denies to
// every guarded package.
package probeadapter

// Program is the program the probe asks probehelper to run.
const Program = "true"
