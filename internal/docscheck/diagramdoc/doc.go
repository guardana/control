// Package diagramdoc renders the generated blocks of the concept pages from
// the listings the code exports: the enforcement-mode table, the kernel's
// fail-closed table and the evidence chain's state diagram. Each block is
// rendered whole from the code, so a page cannot say what the code does not.
package diagramdoc
